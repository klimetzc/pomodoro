import React from "react";
import styled from "styled-components";

const Timer = () => {
  return <TimerWrapper>Timer</TimerWrapper>;
};

export default Timer;

const TimerWrapper = styled.div`
  background-color: rgba(255, 255, 255, 0.1);
  padding: 50px;
`;
